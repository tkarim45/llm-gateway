"""llm-gateway — a production LLM API gateway: API-key auth, per-tenant rate limiting and token
quotas, ordered provider failover (AWS Bedrock → Anthropic API), streaming passthrough, per-tenant
cost metering, and an append-only audit log."""
__version__ = "0.1.0"
